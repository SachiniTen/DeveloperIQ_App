apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-deployment-usercommits-check
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-commits-servicezz
  template:
    metadata:
      labels:
        app: user-commits-servicezz
    spec:
      containers:
      - name: user-commits-serviceczz
        image: 780417121799.dkr.ecr.us-east-1.amazonaws.com/github_user_commits_service:latest

---
apiVersion: v1
kind: Service
metadata:
  name: user-commits-servicezz
spec:
  selector:
    app: user-commits-servicezz
  ports:
    - protocol: "TCP"
      port: 80
      targetPort: 5000
  type: LoadBalancer
